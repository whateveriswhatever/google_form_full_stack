import BriefContent from "../sections/MainSections/BriefContent";

const Main: React.FC = () => {
  return (
    <div id="main" className="w-full flex flex-col justify-center items-center">
      <BriefContent />
    </div>
  );
};

export default Main;
